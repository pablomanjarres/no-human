# tasks/

One markdown file per unit of planned work: the problem, the acceptance
criteria, the decisions already made. Written to be handed to a fresh Claude
Code session with no other context.

A task file is deleted (or moved to a "done" note) when its PR merges — the
directory is a queue, not an archive. Durable knowledge belongs in `docs/`.
